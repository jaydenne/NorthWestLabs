@model IEnumerable<NorthWestLabs.Models.AssayOrder>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AssayOrderID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProtocolNotebook.AssayName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.WorkOrder.WorkOrderID)
        </th>
        <th>
            LTNumber-SequenceCode
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Compound.CompoundName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PriorityLevel.PriorityLevelID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AssayOrderID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProtocolNotebook.AssayName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.WorkOrder.WorkOrderID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Compound.LTNumber) - @Html.DisplayFor(modelItem => item.Compound.SequenceCode)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Compound.CompoundName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PriorityLevel.PriorityLevelID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AssayOrderID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AssayOrderID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AssayOrderID })
        </td>
    </tr>
}

</table>
